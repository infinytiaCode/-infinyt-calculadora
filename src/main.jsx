import React from "react";
import ReactDOM from "react-dom/client";
import InfinytCalculatorLightDash from "./InfinytCalculatorLightDash.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <InfinytCalculatorLightDash />
  </React.StrictMode>
);
